import 'package:datacoup/export.dart';

class QuizStatisticsWidget extends StatelessWidget {
  const QuizStatisticsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: blueGreyLight,
    );
  }
}
